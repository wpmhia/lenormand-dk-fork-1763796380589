[build]
builder = "nixpacks"
buildCommand = "rm -rf .next 2>/dev/null; npm run build"

[deploy]
startCommand = "npm start"
port = 8080
healthCheckPath = "/"
restartPolicy = "onFailure"
maxRetries = 3

[processes]
web = "npm start"
